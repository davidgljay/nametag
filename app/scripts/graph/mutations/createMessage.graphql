mutation CreateMessage($message: CreateMessageInput!) {
  createMessage(message: $message) {
    message {
      id
      text
      createdAt
      saved
      author {
        icon
        id
        name
      }
      recipient {
        icon
        id
        name
      }
    }
    errors {
      translation_key
    }
  }
}
