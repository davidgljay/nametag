#import "../fragments/badgeView.graphql"

query badgeTemplateQuery($id: ID!){
  template(id:$id) {
    id
    description
    name
    icon
    granter {
      id
      name
      description
      type
    }
  }
  me {
    displayNames
    icons
    badges {
      ...badgeView
    }
  }
}
