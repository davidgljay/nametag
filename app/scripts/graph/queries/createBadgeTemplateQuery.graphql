#import "../fragments/badgeView.graphql"

query createBadgeTemplateQuery($granter: ID!) {
  granter(id:$granter) {
    id
    name
    description
    image
    type
  }
  me {
    id
    displayNames
    icons
    badges {
      ...badgeView
    }
  }
}
