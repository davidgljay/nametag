#import "../fragments/badgeView.graphql"

query createTemplateQuery($granter: String!) {
  granter(urlCode:$granter) {
    id
    name
    description
    image
    type
  }
  me {
    id
    displayNames
    icons
    badges {
      ...badgeView
    }
  }
}
