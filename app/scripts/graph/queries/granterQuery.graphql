#import "../fragments/nametagView.graphql"

qeury granterQuery($urlCode: String!) {
  granter(urlCode:$urlCode) {
    id
    name
    description
    image
    notifications {
      text
      createdAt
      type
      nametag {
        ...nametagView
      }
    }
    templates {
      id
      name
      icon
      description
      badges {
        id
        notes {
          text
          date
        }
        defaultNametag {
          ...nametagView
        }
      }
    }
  }
}
