#import "../fragments/nametagView.graphql"

query granterQuery($urlCode: String!) {
  granter(urlCode:$urlCode) {
    id
    name
    description
    image
    badgeRequests {
      id
      status
      createdAt
      template {
        name
        icon
        description
      }
      nametag {
        id
        name
        bio
        icon
        badges {
          ...badgeView
        }
      }
    }
    templates {
      id
      name
      icon
      description
      badges {
        id
        notes {
          text
          date
        }
        defaultNametag {
          ...nametagView
        }
      }
    }
  }
  me {
    id
  }
}
