#import "../fragments/badgeView.graphql"

query templateQuery($id: ID!){
  template(id:$id) {
    id
    description
    name
    icon
    granter {
      id
      name
      description
      type
    }
  }
  me {
    displayNames
    icons
    badges {
      ...badgeView
    }
  }
}
