#import "../fragments/badgeView.graphql"

query templateQuery($id: ID!){
  template(id:$id) {
    id
    description
    name
    image
    granter {
      id
      name
      description
      type
    }
  }
  me {
    displayNames
    images
    badges {
      ...badgeView
    }
  }
}
