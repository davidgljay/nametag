subscription messageAdded ($roomId: ID!, $nametagId: ID!) {
  messageAdded(roomId: $roomId, nametagId: $nametagId) {
    id
    text
    createdAt
    saved
    author {
      id
      name
      icon
    }
    recipient {
      id
      name
      icon
    }
  }
}
