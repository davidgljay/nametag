mutation CreateDonations($donation: CreateDonationInput!, $nametagId: ID!) {
  createDonation(donation: $donation, nametagId: $nametagId) {
    id
    errors {
      translation_key
      message
    }
  }
}
