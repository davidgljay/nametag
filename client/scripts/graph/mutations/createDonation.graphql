mutation CreateDonations($amount: Int!, $nametagId: ID!, $token: String!, $note: String) {
  createDonation(amount: $amount, nametagId: $nametagId, token: $token, note: $note) {
    id
    errors {
      translation_key
      message
    }
  }
}
