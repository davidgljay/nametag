mutation CreateMessage($message: CreateMessageInput!) {
  createMessage(message: $message) {
    message {
      id
      text
      createdAt
      editedAt
      saved
      author {
        image
        id
        name
      }
      recipient {
        image
        id
        name
      }
      reactions {
        emoji
        nametagId
      }
    }
    errors {
      translation_key
      message
    }
  }
}
