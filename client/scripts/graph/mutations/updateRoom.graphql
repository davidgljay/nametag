mutation UpdateRoom($roomId: ID!, $property: String!, $value: String!) {
  updateRoom(roomId: $roomId, $property: property, $value: value) {
    errors {
      translation_key
      message
    }
  }
}
