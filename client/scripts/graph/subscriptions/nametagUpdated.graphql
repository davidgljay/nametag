#import "../fragments/badgeView.graphql"

subscription nametagUpdated ($roomId: ID!) {
  nametagUpdated(roomId: $roomId) {
    id
    name
    bio
    image
    present
    banned
    badges {
      ...badgeView
    }
  }
}
